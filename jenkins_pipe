#!groovy
// Check njinx properties
properties([disableConcurrentBuilds()])

pipeline {
    agent { 
        label 'master'
        }
    options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
        timestamps()
    }
    stages {
        stage("First step") {
            steps {
                sh 'ssh vagrant@127.0.0.1 -p2222 \'hostname\''
            }
        }
        stage("Second step") {
            steps {
                sh 'ssh vagrant@127.0.0.1 -p2222  \'uptime\''
            }
        }
    }
}
